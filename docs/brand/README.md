# Brand assets

- `pbj-logo-pink.svg` — **the live logo** (brand pink `#ff43a4`, same as the
  app's `--pink` highlighted-tab color). This exact file, base64-encoded as a
  `data:image/svg+xml` URL, is what `firm_settings.logo_url` holds in
  production (swapped 2026-08-04; undo snapshot in `../prod-snapshots/`).
- `pbj-logo-navy.svg` — the remaster as delivered (navy `#0b284c`). The pink
  variant is this file with `#0b284c → #ff43a4`.
- Favicon: `public/favicon.svg` (pink square, white PB monogram).
- Vector sources (.ai/.eps): machine-local at
  `D:\PBJ Accounting\Logo Images\Logo Remaster\` — not committed.
