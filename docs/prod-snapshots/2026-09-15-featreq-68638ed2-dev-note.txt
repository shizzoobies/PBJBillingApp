[2026-09-15 - why you could not find it, and what changed] The button was there, but it only showed on tasks whose repeating setup has "allow skipping" turned on, the same rule as Skip. Only 6 of your 150 repeating setups have that on, so on 95 out of 100 tasks neither button appeared. It also hides on a task you already skipped (the Dobco Client Meeting you skipped yesterday is the one that had the Skip button, so going back to that card shows nothing).

CHANGED (commit 0f68aef): "Push to a new date" now appears on every task you can edit, repeating or one-off, whether or not skipping is allowed for it. Skip keeps its per-setup opt-in exactly as before. A one-off task pushed with no cycle defaults to one month out. Look at any open task card: the button sits in the header next to the task name, on the Checklists page, the Board, and the client page.
