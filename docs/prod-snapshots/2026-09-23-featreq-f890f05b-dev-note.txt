[2026-09-23 - tabs] Done as you asked (commit 7f0adfb): the Plans page now opens with two tabs across the top, Plans and Packages, each showing its count. Plans is the default; click Packages to see only the packages list, with the same Add package button, editor, and Suggest checklists as before. Nothing about how a package works changed, and "+ Add package" on a client's page is the same. Two small extras: the address plans?tab=packages opens straight to the Packages tab, and the "No packages yet" line on a client's page is now a link to it.
