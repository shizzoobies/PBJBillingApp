Shipped 4fcfc5e + bcfea18, 2026-09-14. Next to "Skip this cycle" on a task that allows skipping there is now "Push to a new date". Push keeps the task alive and moves its due date: the date defaults to the next cycle and you can change it (it must be after the current due date and within two years). Same small form as a skip, who could not complete it (me / a colleague / the client) and a written explanation, and the same people are told. Pushed tasks show a small "Pushed · was <date>" marker on the card and sit in the dashboard's review list next to skips, now titled "Skipped and pushed tasks to review", each reading "Pushed to <date>". A push is not a completion and not a skip: the task keeps its place in its cycle, so the next occurrence still generates on schedule even when you push onto that same date.

TWO THINGS TO SETTLE WITH YOU (both left exactly as they are today):
1. Whole phase vs a single subtask. Skip and Push both apply to the whole task (the phase), which is how Skip has always worked. Your draft mentioned a toggle at initiation to push or skip just one subtask instead. If you want that, say so and it is the next step.
2. Sequences. When someone skips or pushes their own task in a sequence, nothing downstream is triggered (same as a skip today: a skipped or pushed step does not count as done for whoever is waiting on it). If you would rather a push or skip release the next person, tell Alex which way.
