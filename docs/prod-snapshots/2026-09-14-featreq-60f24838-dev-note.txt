Shipped 7c52aa6, 2026-09-14. Client Recap now lists only active clients in its client picker. To recap an old client, tick the new "Include inactive clients" box next to the picker: retired clients join the list marked "(inactive)" and their recap loads as before. Untick it and the picker goes back to active clients only. Nothing else on the page changed, and the Invoice Recap was already history-only (a retired client shows there only for a month it actually had an invoice), so it was left alone.
